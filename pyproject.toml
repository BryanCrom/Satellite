[project]
name = "satellite"
version = "0.1.0"
description = "satellite sim"
readme = "README.md"
authors = [
    { name = "Bryan", email = "BryanCrombach@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
satellite = "satellite.__main__:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
